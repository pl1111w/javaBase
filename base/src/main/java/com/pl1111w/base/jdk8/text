Lambda表达式：
1、必须是接口
2、必须只有一个抽象方法 @FunctionalInterface被这个注解修饰只可以声明一个抽象方法
原理（通过反编译.class）：
Lambda表达式在程序运行时会形成一个类。
1、在类中新增一个方法，这个方法(内容名含$)就是lambda表达式中的内容
2、还会形成一个匿名内部类，实现接口，重写抽象方法
3、在接口重写方法会调用新生成的方法


为啥使用方法引用
1、lambda表达式冗余
::方法引用符号
如果lambda表达式所要实现的方案，已经有其他方法实现，那么就可以使用方法引用

Stream
如果不调用Terminate方法，middle不会执行